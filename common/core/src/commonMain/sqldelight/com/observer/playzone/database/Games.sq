CREATE TABLE GAMEDBPLAYZONE (
    gameId INTEGER PRIMARY KEY NOT NULL,
    title TEXT NOT NULL
);

insert:
INSERT INTO GAMEDBPLAYZONE(gameId, title)
VALUES (?, ?);

selectAll:
SELECT *
FROM GAMEDBPLAYZONE;